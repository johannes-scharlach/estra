// The Expo template imports global.css for its web target. Expo generates
// expo-env.d.ts with these declarations on first run; declaring them here
// keeps `tsc --noEmit` green in CI without a build step first.
declare module '*.css';
