# Expo's Metro bundler and native autolinking cannot follow pnpm's default
# symlinked node_modules layout. `hoisted` gives an npm-style flat tree.
# See: https://docs.expo.dev/guides/monorepos/
node-linker=hoisted
